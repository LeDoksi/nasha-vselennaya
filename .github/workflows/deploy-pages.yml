name: Публикация на GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Забрать код
        uses: actions/checkout@v4
      - name: Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
            - name: Собрать app.js из src/
        run: node build.js
      - name: Вставить S3-ключи в index.html
        run: |
          sed -i 's|window.YANDEX_S3_KEY = ""|window.YANDEX_S3_KEY = "${{ secrets.YANDEX_S3_KEY }}"|' index.html
          sed -i 's|window.YANDEX_S3_SECRET = ""|window.YANDEX_S3_SECRET = "${{ secrets.YANDEX_S3_SECRET }}"|' index.html
      - name: Собрать публикуемые файлы
        run: |
          mkdir -p _site/fonts
          cp index.html app.js styles.css icon.svg manifest.webmanifest _site/
          cp -r fonts/* _site/fonts/
      - name: Загрузить артефакт
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Опубликовать
        id: deployment
        uses: actions/deploy-pages@v4
