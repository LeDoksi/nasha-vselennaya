# 🧠 Памятка проекта «Наша вселенная»

> **Зачем этот файл.** Чтобы любой новый чат (или человек) мог прочитать его и
> мгновенно войти в контекст: что это за проект, как устроен, что уже сделано,
> что в планах. Новый чат: «Прочитай `PROJECT-MEMORY.md` в папке наша-вселенная».

---

## 0. ⚡ Снимок состояния (08.08.2026) — старт для нового чата

> **Новому чату:** прочитай этот раздел — и всё, что нужно для старта, у тебя есть.
> Дальше по файлу — детали (схема, механики, история, тесты).

**Проект:** личный сайт «Наша вселенная» (Гоша 💜 Даша). Статичный `index.html` +
`styles.css` + `app.js`. `app.js` **собирается** из `src/*.js` (`node build.js`,
12 модулей по зонам ответственности; собранный файл лежит в репозитории, так что
двойной клик по `index.html` работает без сборки). Данные — localStorage + фото
в IndexedDB (схема v8). Открыть: двойной клик по `index.html`. Тест: сначала
`node build.js`, затем `node tests\uni-smoke.js app.js` (339 проверок, зелёные) +
`node tests\uni-dnd.js app.js` (18 — drag&drop через события, мини-DOM) +
`node tests\repro-event-btn.js app.js` (15). Либо `npm test` или двойной клик
по `run-tests.cmd` (он сам пересобирает app.js). Итого **372 проверки**.
Путь: `C:\Users\Georgy\OneDrive\Рабочий стол\нейронка\наша-вселенная\`

**Фаза A (в работе): фото переезжают из localStorage в IndexedDB.**
- Новый модуль `src/05-photostore.js`: два бэкенда — `IDBPhotoStore` (браузер,
  зашифрованные AES-GCM блобы) и `MemoryPhotoStore` (тесты/фолбэк). API:
  `put/getFull/getThumb/getMeta/delete/all/exportBlobs/importBlobs/clear/migratePhotos/refreshSizes`.
- В `src/00-core.js` `DB_VERSION = 6`, `defaultDB()` добавляет `moods: []`.
- `10-vault.js`: `createVault`/`unlockWith` вызывают `photoStore.migratePhotos(db)`
  + `refreshSizes()` + `warmThumbCache()`; `lock()` очищает store (`clearPhotoStore`)
  и кэш миниатюр (`clearThumbCache`). `initPhotoStore()` гарантирует бэкенд.
- Миграция переносит `data`-URL в store и даёт фото `id`, а затем **удаляет
  `p.data` из памяти** — base64 не живёт в сейфе/localStorage. Ссылки событий
  `ev.photos` с data-URL переводятся на id фото; `unlockWith` после миграции
  делает `save()`, чтобы почищенный db закрепился в сейфе. Рендеры работают
  через кэш миниатюр (`warmThumbCache` после разблокировки) и `photoUrl()`.
- Рендеры переведены на `photoSrc(p)` (синхронный превью **только из кэша
  миниатюр**) и `photoUrl(p)` (async: кэш → store → data). Легаси `p.data`
  из рендеров полностью убран — фото живёт в photoStore. `warmThumbCache()`
  прогревает кэш после разблокировки.
- При загрузке фото (галерея и события) сразу генерируется **WebP-миниатюра**
  (`makeThumbBlob`, ~256px через canvas; без canvas — graceful fallback),
  полный блоб + миниатюра пишутся в store, base64 убирается из памяти.
- События: `ev.photos` хранит **id фото** (v6+). Рендер (`photoByRef`/`evThumbs`,
  фильтры `eventsForPhoto`/`renderEventBar`, `deletePhoto`) работает только по
  id; data-URL поддерживаются лишь на входе миграции/нормализации (v4/v5 →
  v6) и для сиротских рефов без фото в галерее (показ напрямую).
  `addEventPhotosToGallery` синхронная, пишет копию в store в фоне.
- Бэкап: `exportData` добавляет секцию `photos` (зашифрованные блобы),
  `importData` восстанавливает их в store.
- В настройках — строка «Фото-хранилище» (`#photoStorageInfo`) с количеством
  фото и объёмом.

**Что уже сделано и работает:**
- **Главная:** счётчик «дней вместе», комплимент дня 💌, таймер до ближайшего
  события/свидания ⏳ (баг «…» исправлен), блок «Наша история» (кольцо прогресса +
  коллаж из случайных фото с асимметрией + статистика), конфетти 🎉.
- **Свидания:** назначает **вошедший** (селекта «кто» нет), отвечает только
  приглашённый («позвал/позвала», Да/Нет), оба «да» → конфетти.
- **Календарь:** события, повтор каждый год, редактирование ✏️, панель дня,
  быстрый выбор месяца/года, длительные события (диапазон дней с подсветкой),
  фото в событиях (общий лейбл «📅 События», название — подпись на фото);
  после сохранения календарь сам переходит на месяц события. Месяц/год — только
  селекты в шапке (текст-заголовок убран, дублирования нет); в ячейках рядом с
  эмодзи видно **название события** (до 2 + «+N»); в панели дня у события есть
  кнопка 📷 быстрого добавления фото (без модалки, та же логика галереи).
  Кнопка **«⏭ К ближайшему событию»** (`nextUpcoming()`/`jumpToNearestEvent()`)
  — переключает месяц на ближайшее событие/свидание (учитывает ежегодные повторы
  и идущие сейчас диапазоны), выделяет день. `updateNearestJump()` (вызывается из
  `renderCalendar`) показывает кнопку и плашку `#jumpInfo` «что за событие»
  **только когда ближайшее событие НЕ в показываемом месяце**; в месяце самого
  ближайшего события их нет (даже если в этом месяце есть другие события, но не
  ближайшее — кнопка есть). Открытие вкладки календаря показывает **текущий месяц**
  без прыжка и без автовыбора дня.
- **Заметки** 💌: на карточке виден **автор** (👦/👧, старые — 💜), **✏️-правка**
  (кнопка или двойной клик, инлайн-текст + «💜 Сохранить»), **drag&drop**
  перестановка (пересчёт `order`), закрепление 📌 и удаление — **может любой**.
  Бросок работает и на фон списка (ниже последней / выше первой карточки),
  dropEffect задаётся явно, чтобы Chrome не отменял drop.
- **Вишлист:** разделён по людям («Хотелки Гоши» / «Хотелки Даши»), секция
  «хочу подарить» **убрана** (подарок — сюрприз), фото+ссылка опциональны.
  **«Исполнено другим»:** свою хотелку исполнить нельзя — только партнёр
  (на своей карточке подсказка «Только … исполнит 💜»), на исполненной — бейдж
  «💜 Исполнено Гошей/Дашей · дата» (`doneBy`/`doneAt`), снять отметку может
  только исполнивший, исполненные уходят вниз списка.
- **Фото:** **лейблы 🏷 вместо альбомов** — несколько лейблов на фото, фильтр
  сверху («Все фото» или один лейбл), выбор нескольких фото и массовое добавление
  лейбла, создание/удаление лейблов без вреда фото. **Drag&drop:** перетаскивание
  фото на кнопку лейбла (лейбл получает и перетаскиваемое фото, и все отмеченные;
  кнопка подсвечивается), крестик ✕ на бейдже фото убирает лейбл с конкретного
  фото, после действий с выбранными фото выделение снимается. Переименование ✏️ **убрано**.
  У фото `effectAllowed='copyMove'`, у лейбла `dropEffect='copy'` — иначе Chrome
  отменял drop. **Обратное направление:** чип лейбла (draggable) можно перетащить
  прямо на фото — лейбл навесится на фото (и все отмеченные).
  Закрепление ⭐, drag&drop порядка, лайтбокс, мобильная лента. Фото из событий —
  **витрина «📅 События»** (чип в фильтре): фильтр **кнопками «год → месяц →
  событие»** с счётчиками фото — кнопки появляются по мере выбора, повторный клик
  сбрасывает уровень, есть «✕ Сбросить»; подпись названием на фото; лейблы-названия
  не плодятся. Удаление фото `deletePhoto(id)` **чистит его и в событиях** —
  «мёртвых» миниатюр в календаре не остаётся.
- **Доступность date-picker:** кастомный календарь получил aria-паттерн
  «dialog + grid»: `role=dialog` + `aria-label` у попапа, `aria-label` у кнопок
  дней («9 августа 2026 года»), `aria-current="date"` у сегодня, roving tabindex
  (ровно одна ячейка `tabindex=0`), клавиатура — стрелки, Home/End,
  PageUp/PageDown (± месяц, Shift — ± год), Enter/Space, Esc; выбранная дата
  озвучивается скринридером через `#dpLive` (`role=status`, `aria-live=polite`).
- **«Отключить анимации» в настройках:** переключатель ставит `data-motion` на
  `<html>` (`reduced`/`full`); без явного выбора — уважаем системный
  `prefers-reduced-motion`; `full` перекрывает систему. Сердечки и конфетти при
  выключенных анимациях не создаются вообще (JS-гард).
- **Фото: дебаунс рендера** — несколько `renderPhotos()` в одном кадре
  схлопываются в один через `requestAnimationFrame` (без rAF — синхронно).
- **Тёмная тема 🌙:** переключатель в шапке и настройках; оверрайды для карточек,
  кнопок, календаря, заметок; **активное меню красится градиентом** (фикс
  специфичности CSS).
- **Настройки:** скачать/загрузить копию JSON + напоминание о бэкапе (30 дней),
  счётчик занятого места.
- **Сейф и вход 🔐:** все данные шифруются (AES-GCM) и живут в `universe_vault`.
  Свой пароль + пароль партнёра, замок/разблокировка, экспорт/импорт сейфа
  (включая старые открытые бэкапы — они сразу шифруются). Вход после перезапуска
  работает.
- **Аудит 07.08.2026 (цвета / анимации / логика):** контраст подтянут к WCAG AA
  (мелкий текст на `--pink-ink`/`--violet-ink`, темнее градиенты кнопок),
  исправлены баги (сброс дня календаря, `migrateDB()` при импорте, авто-пересчёт
  таймера), добавлены Esc/Enter, `aria-pressed`, пауза таймеров в скрытой вкладке,
  системная тема при первом запуске. Анимации теперь **можно отключить**
  переключателем в настройках (`data-motion` на `<html>`); без явного выбора —
  уважаем системный `prefers-reduced-motion`, `full` перекрывает систему.

**Ключевые технические факты (обязательно читай при правках):**
- `DB_VERSION = 5`; `migrateDB()` конвертирует старый `album` фото → `labels`
  (массив), фото из событий подписывает общим лейблом
  `EVENT_LABEL = '📅 События'` (v4), а заметкам добавляет `order` для drag&drop
  (v5). Схема всех данных — в разделе 4.
- Фото в localStorage хранятся как dataURL (jpeg, max 900px, качество 0.82),
  лимит localStorage ~5 МБ.
- `renderPhotos()` — с дебаунсом: несколько вызовов в одном кадре схлопываются
  в один через `requestAnimationFrame` (без rAF — синхронно).
- Коллаж «Наша история»: `historyPhotosHtml()` (в `src/30-home.js`) рисует в блоке
  `#progressRing` до 3 случайных фото (слоты `HISTORY_PHOTO_SLOTS` — разный размер,
  поворот, вертикальный сдвиг), выбор стабилен в течение дня (`daySeed`/`mulberry32`),
  фото кликабельны (lightbox) и с fallback `data-photo-src` (`hydratePhotoImgs`).
- Свидания: `canAnswer = !from || from === 'both' || from !== who`.
- Смоук-тест в песочнице (`new Function`) — проверяет рендеры и состояние,
  клики-заглушки. **После любых правок: `node tests\uni-smoke.js app.js` и
  `node --check app.js`.**
- Сейф: `localStorage['universe_vault']` = `{ ver:1, db: AES-GCM-enc, keys:[wrap] }`.
  `keys[i] = { who, k: wrapped-AES-key, s: PBKDF2-salt, iter, iv, a }` — **salt
  обязан сохраняться в wrap (поле `s`)** во всех `createVault`/`savePassFor`/`changePass`,
  иначе после перезапуска ключ не восстановить. При разблокировке AES-key
  импортируется как **extractable** (`true`) — иначе не выпустить wrap для пароля
  партнёра. Открытый `universe` после миграции в сейф удаляется.
- Витрина «📅 События»: фильтр — объект `eventFilter = {year:'', month:'', title:''}`
  (раньше была строка). Уровни кнопок: `#eventYears`, `#eventMonths`, `#eventTitles`;
  клики ловит **один делегат на `document`** (`data-ev-year/month/title/reset`),
  который при выборе сбрасывает более глубокие уровни. `renderEventBar()` рисует
  только события, у которых в `ev.photos` реально есть фото из галереи.
  Удаление фото — `deletePhoto(id)`: убирает из `db.photos` и из `ev.photos` всех
  событий (пустые `photos:[]` удаляются), затем `renderCalendar()`.

**Планы / что дальше — дорожная карта «Наша вселенная 2.0» (утв. 07.08.2026):**
- **Фаза A — Данные (ГОТОВО):** фото в IndexedDB + WebP + миниатюры + EXIF-дата,
  бэкап v2 (фото в файле), схема v6 (`moods`), модуль `05-photostore.js`.
  Сделано: модуль, миграция при unlock, `photoSrc`/`photoUrl` в рендерах,
  `ev.photos` → id, **полный отказ от `p.data`**, WebP-миниатюры при загрузке,
  чистка рендеров от легаси, бэкап с секцией `photos`, **EXIF-дата при загрузке**
  (`takenAt` в запись фото и мету стора — `70-photos.js`), тесты (279 ✓).
  Осталось: миграция wish-фото (`w.data`), при желании — WebP-кодирование полного.
- **Фаза B — Душевные фичи (ГОТОВО):** «В этот день», кольцо прогресса,
  лента «Память» (таймлайн-дерево, вкладка + модуль `35-memory.js`).
  Сделано: `photoDate()` (EXIF ИЛИ дата события, `ts` — нет), `onThisDayItems`/
  `renderOnThisDay` (виджет на главной), `renderProgressRing` (SVG-кольцо до
  годовщины), `memoryByDay`/`renderMemory` (таймлайн-дерево: фото с горизонтальной
  прокруткой, прошедшие свидания, события с фото), трекер настроения УДАЛЁН
  (пользователь: «избавимся от "Как прошёл день"»), заметки и фото без EXIF
  НЕ попадают в «Память», вкладка «Память» в навигации, тесты Фазы B.
- **Фаза C — Апп-оболочка (ПОЧТИ ГОТОВО):** hash-роутинг (#/view), **нижняя
  навигация на мобильных** (5 + «Ещё»-шторка: главные — Главная, Календарь,
  **Память**, Списки, Фото; в «Ещё» — Заметки, Хотелки, Песня, Настройки;
  `BOTTOM_PRIMARY`/`BOTTOM_MORE` в `20-theme-nav.js`, клоны кнопок шапки, шторка
  `#navSheet`, Esc/клик по фону закрывают; на <=820px шапка-пилюли скрывается),
  **светбокс 2.0** (стрелки ‹›,
  свайп/пинч, зум 🔍/двойной клик/клавиши +−0, счётчик «N / M»; модуль
  `85-lightbox.js`; листает внутри группы/вкладки; data-URL хотелок открываются
  напрямую), **PWA-манифест + icon.svg** (`manifest.webmanifest`, `theme-color`).
  Осталось из Фазы C: service worker (нужен HTTP(S)) — после выкладки.
  Тесты: 369 (uni-smoke) + 25 (uni-dnd) + uni-hash (старт по #/wishlist без TDZ).
- **Фаза D — Дизайн-пакет:** локальный шрифт (woff2 с кириллицей в репозитории),
  View Transitions API, aurora-фон, glass-карточки, тёмная тема 2.0, пустые состояния,
  микро-анимации, имя бэкапа с датой.
- В резерве (не выбраны): открытки из события+фото, глобальный поиск.
- После: ☁️ облачная синхронизация (телефон ↔ компьютер).

**План из чата (08.08.2026, в работе):**
- **1. Кастомные списки (СДЕЛАНО, схема v8):** вместо жёстких «Покупки/Дела» —
  произвольные списки (`db.lists`: `{id, name, items[]}`). Миграция переносит
  старые `shopping`/`todos` в списки и чистит легаси-поля. Новые функции:
  `createList`, `addListSubtask`, `toggleSubtask`, `delSubtask`, `completeList`
  («✔ Выполнить список» с confirm). Рендер — `renderLists` в `#listsWrap`,
  клики — `data-toggle-item` / `data-del-item` / `data-list-add` /
  `data-list-complete`, Enter в поле подзадачи.
- **2. done через календарь (СДЕЛАНО):** панель дня (вкладка «Календарь») —
  у свидания кнопка **✅ «Свидание прошло» / 💗 «Снять отметку»**
  (`data-done-date`, общий `toggleDateDone(id)` для главной и календаря).
  Теперь прошлое свидание можно пометить done → оно попадает в «Память».
- **3. Свидания в «В этот день» (СДЕЛАНО):** `onThisDayItems` отдаёт и даты
  свиданий (`kind:'date'`), чипы показываются в `history-otd` рядом с событиями
  прошлых лет (как события; фильтра по `done` нет — показывается всё,
  что было «в этот день» в прошлые годы).
- **4. Сворачивание фото в «Памяти» (СДЕЛАНО):** в ряду дня и в карточках
  события/свидания сразу видны 3 фото (`MEMORY_PHOTOS_PREVIEW`), остальные
  скрыты за кнопкой «Показать ещё N» → «Свернуть» (`memoryPhotosHtml` +
  `toggleMemoryPhotos`, делегат на `[data-tl-expand]`). Все фото остаются в DOM
  (`display:none`), лайтбокс работает по всем.
- **5. UX-полировка списков (СДЕЛАНО):** drag&drop перестановка карточек
  списков (паттерн заметок: `reorderListIds`/`listDropPosition`, порядок —
  сам `db.lists` и сохраняется), новые списки появляются сверху (`unshift`),
  выполненные подзадачи улетают вниз (`sortListItems` — устойчивая сортировка
  в `toggleSubtask` и в рендере).

**Решения (приняты 07.08.2026):** бэкап включает фото (самодостаточный файл);
«Память» — отдельная вкладка; нижняя навигация «5 + Ещё»; фото хотелок остаются
встроенными (их мало). **«В этот день» для фото — только 2 источника даты:
EXIF-дата снимка ИЛИ дата события, к которому привязано фото. Если есть только
дата загрузки на сайт (`ts`) — фото НЕ показываем.** Облачная синхронизация —
в самую последнюю очередь (сначала GitHub, потом фичи; см. порядок Фаз ниже).

**Порядок работы (утв. 07.08.2026):** C1 hash-роутинг → Фаза B (душевные фичи)
→ остальная Фаза C (нижняя навигация, светбокс, PWA) — **сделано** (08.08.2026).
Остался service worker — только после выкладки на HTTP(S). Далее — Фаза D
(дизайн-пакет).

---

## 1. Что это

Личный сайт для влюблённой пары **Гоша 💜 Даша**. Работает **полностью локально**,
без интернета, без сборки и зависимостей: это статичная HTML+CSS+JS страница,
данные — в localStorage браузера.

- **Путь:** `C:\Users\Georgy\OneDrive\Рабочий стол\нейронка\наша-вселенная\`
- **Открыть:** двойной клик по `index.html`
- **Язык интерфейса:** русский, всё нежное 💜

## 2. Структура

```
наша-вселенная/
├── index.html          ← разметка всех вкладок и модалок
├── styles.css          ← стили + адаптив (медиа-запросы)
├── app.js              ← собран из src/ (node build.js); готовый лежит в репозитории
├── build.js            ← конкатенация src/*.js → app.js
├── src/                ← 11 модулей (сборка конкатенацией в этом порядке):
│   ├── 00-core.js      ← константы, утилиты (esc/safeUrl/store), крипто-ядро, миграции
│   ├── 05-photostore.js← фото: IndexedDB/память, шифрование блобов, миниатюры, EXIF
│   ├── 10-vault.js     ← хранилище, сейф, вход/пароли, замок
│   ├── 20-theme-nav.js ← тема и навигация по вкладкам
│   ├── 30-home.js      ← главная: счётчик, комплимент, таймер, конфетти, свидания
│   ├── 40-calendar.js  ← календарь, панель дня, date-picker, события
│   ├── 50-notes.js     ← заметки (drag&drop, закрепление)
│   ├── 60-lists-wishes.js ← списки, хотелки, глобальные клики
│   ├── 70-photos.js    ← фото, лейблы, витрина событий, drag&drop
│   ├── 80-settings.js  ← настройки, бэкап/импорт, пароли
│   └── 90-effects-init.js ← сердечки, запуск
├── tools/split.js      ← нарезка app.js → src/ (разово, при рефакторинге границ)
├── package.json        ← npm run build / npm test / npm run check
├── run-tests.cmd       ← двойной клик = сборка + смоук-тест
├── README.md           ← «человеческая» инструкция
├── PROJECT-MEMORY.md   ← этот файл (памятка для новых чатов)
├── .gitignore          ← git: музыка и системные файлы не коммитятся
├── tests/
│   └── uni-smoke.js    ← смоук-тест (203 проверки, запуск node tests\uni-smoke.js app.js)
└── music/
    └── nasha-pesnya.mp3 ← наша песня (положить вручную)
```

**Git-репозиторий есть** (ветка `main`, первый коммит сделан). Песня `music/`
в git не попадает (личная, кладётся вручную). Git установлен через winget:
`C:\Program Files\Git\cmd\git.exe` (в текущей сессии может быть не в PATH —
используй полный путь). Идентичность для коммитов локальная:
`user.name=Georgy`, `user.email=georgy@local`. Команды:
`git add -A && git commit -m "..."`, `git log --oneline`, `git status`.

## 3. Вкладки (views)

`Главная · Календарь · Заметки · Списки · Хотелки · Фото · Песня · Настройки`
Переключение: кнопки `.nav-btn` с `data-view`, функция `go(view)`.

## 4. Хранилище (localStorage)

| Ключ | Что хранит |
|---|---|
| `universe` | главный объект: `{ version, events, notes, shopping, todos, photos, dates, wishlist, labels, backupDate }` |
| `universe_vault` | зашифрованный сейф: `{ ver:1, db (AES-GCM), keys: [{ who, k, s, iter, iv, a }] }` |
| `universe_user` | кто сейчас у сайта: `'gosha'` или `'dasha'` |
| `universe_theme` | тема: `'light'` или `'dark'` |

- **Версия схемы:** `DB_VERSION = 5` в объекте `db`. Загрузка через `migrateDB()`:
  добавляет новые поля старым данным (wishlist, backupDate, labels, version,
  `order` заметок) и конвертирует старые альбомы фото в лейблы. Правь `migrateDB`,
  когда меняешь форму данных, — и старые данные переживут апдейт.
- **Лимит** ~5 МБ (localStorage). Фото сжимаются до jpeg 900px / качество 0.82.
- Копия — вручную: **Настройки → Скачать копию** (JSON); при экспорте ставится `db.backupDate`,
  и Настройки показывают, сколько времени прошло с последней копии. Облака пока нет.

### Формы данных
```js
events:   { id, title, date:'YYYY-MM-DD', emoji, repeat: bool,
            endDate:'YYYY-MM-DD'|null (долгие события, repeat принудительно false),
            photos:[dataURL]|undefined (фото события; дублируются в db.photos с лейблом = title) }
dates:    { id, date, time:'HH:MM', from:'gosha'|'dasha'|'both'|undefined(legacy),
            responses:{gosha:null|'yes'|'no', dasha:null|'yes'|'no'},
            place, note, emoji, done: bool }
notes:    { id, text, ts, pinned: bool, author:'gosha'|'dasha'|undefined(legacy),
            order: num (drag&drop; старым назначается миграцией v5) }
shopping: { id, text, done: bool }   // todos — так же
wishlist: { id, text, link:'', data:dataURL|null,
            owner:'gosha'|'dasha', done: bool, ts }
            // старое поле type:'want'|'give' больше не используется (хранится, игнорируется)
photos:   { id, data: dataURL, title, labels: [строка], pinned: bool, ts, order: num }
labels:   [строка] — общий список лейблов (для кнопок-фильтров)
          // старое поле album:'строка' хранится у старых фото, игнорируется
```
- `START_DATE = '2026-03-30'` — старт счётчика «дней вместе» (функция `daysTogether()`).
- Порядок фото: `photoSort` — закреплённые сверху, дальше по `order` (по возрастанию).
  Новые фото получают `order:0`; drag&drop пересчитывает `order` всем фото списка.
- Легенда: `from` = кто приглашает. Свидания без `from` — «старые», отвечать могут оба.

## 5. Ключевые механики

### Вход, сейф и текущий пользователь
- Все данные — в **зашифрованном сейфе** `universe_vault`: общий AES-GCM ключ,
  обёрнутый (wrap) для каждого пароля (PBKDF2 → AES). Первый запуск мигрирует
  старый открытый `universe` в сейф и удаляет его.
- Разблокировка: `unlockWith(who, pass)`; смена/добавление пароля: `changePass` /
  `savePassFor`; замок `lock()` стирает ключ и данные из памяти.
- **Salt (PBKDF2) обязан храниться в wrap (`keys[].s`)** — был баг: без него после
  перезапуска сейф не открывался. AES-key при разблокировке **extractable** (`true`),
  иначе не сделать wrap для пароля партнёра.
- Текущий пользователь: `getUser()`/`setUser(u)`; чип в шапке `#userChip`.
- **Свидание/хотелка всегда от имени вошедшего** — селектов «кто» в модалках нет.

### Блок «Наша история» на главной (кольцо + коллаж + статистика)
- Обёртка `.hero-zone` вокруг `.hero-card` — без парящих фото (герой чистый).
- `renderProgressRing()` (в `src/35-memory.js`) рисует в `#progressRing`:
  `.history-main` = кольцо прогресса + `.ring-info` (годы/дни вместе) +
  `.history-photos` (коллаж), и `.history-stats` под ними.
- `historyPhotosHtml()` (в `src/30-home.js`): `HISTORY_PHOTO_SLOTS` — **3 слота**
  (разный размер 72–100px, повороты `rotate`, верхний сдвиг; без ровного ряда).
  Показываются `min(3, db.photos.length)` фото; `pickHistoryPhotos()` тасует
  копию `db.photos` ГПСЧ `mulberry32`, засеянным датой (`daySeed`) — коллаж
  **не перетасовывается** при каждом рендере в течение дня.
- **Приоритет — фото «в этот день»** (`onThisDayPhotos()` → `onThisDayItems()`,
  EXIF/дата события): они встают первыми в слоты, остальное — случайные.
  Когда такие есть — на коллаже бейдж `✨ В этот день` (`.hp-badge`).
- **«В этот день» без отдельного виджета:** события прошлых лет показываются
  чипами `.hs-otd-chip` в строке `.history-otd` прямо в блоке «Наша история»
  (строка появляется только когда такие события есть). Отдельный `#onThisDay`
  удалён — он дублировал коллаж.
- **Кнопка «🎲 Перемешать»** (`.hs-shuffle`, делегирование на `#progressRing`):
  `shuffleHistoryPhotos()` заново тасует случайную часть (фото «в этот день»
  остаются), результат фиксируется на день в `historyCollage = {day, sig, ids}`.
  `sig` — сигнатура состава галереи: при добавлении/удалении фото коллаж
  пересобирается (старый кэш не «залипает»).
- `photoSrc(p)` из кэша миниатюр; если кэш холодный — `<img data-photo-src>`,
  `renderHome()` дозаливает через `hydratePhotoImgs($('#progressRing'))`.
- Фото кликабельны: `data-photo` → глобальный обработчик открывает lightbox.
- Статистика `plural(n, one, few, many)` — чипы 📸 фото / 📅 событий / 💘 свиданий /
  📝 заметок. **Хотелки — чип с прогрессом**: `🎁 3/7` + мини-полоска `.hs-bar`
  (ширина = % исполненных из `db.wishlist`). Рядом — кнопка «🎲 Перемешать».
- Адаптив: ≤820px коллаж компактнее (70px), ≤480px — вниз на всю ширину (64px);
  бейдж на узких экранах переезжает вниз. Лента `#mobilePhotosGrid` под главной
  осталась как была (≤820px).

### Свидания и ответы (важно!)
- Кнопка «💘 Назначить свидание» → модалка: дата, время, место, идея, эмодзи.
  «Дата» и «Во сколько» — в `.two-col .field` (label над полем, поля во всю
  ширину колонки — выровнены в одну линию).
  **Селекта «кто приглашает» нет** — свидание всегда от имени вошедшего
  (`from: getUser()` в `saveDateFromModal()`). Вариант «Вместе» не создаётся
  (старые свидания `from:'both'` рендерятся для совместимости).
- Пригласивший **уже согласен** — у него в статусе «💌 позвал/позвала», кнопок Да/Нет нет.
- Отвечает **только приглашённый**: `canAnswer = !from || from === 'both' || from !== who`.
- Если `from === 'both'` — отвечать могут оба.
- Ответ — переключатель: повторный клик снимает выбор (`resp[who] = val` → `null`).

### Прочее
- Календарь: события с `repeat` показываются каждый год; клик по дню → панель справа.
  События можно **редактировать** (кнопка ✏️ → `openEventModal(id)` с `editingEventId`).
- Главная: блок напоминаний «Скоро важное событие» **удалён** (дублировал таймер ⏳).
- Заметки: на карточке — **автор** (👦/👧, legacy без `author` → 💜 «Наши»),
  **✏️-правка** (кнопка `data-edit-note` или двойной клик → инлайн-текст с
  `data-save-note`/`data-cancel-note`), **drag&drop** (карточки `draggable`; на
  `drop` пересчитывается `order` всем заметкам). Сортировка: закреплённые сверху,
  дальше по `order`, в конце по времени. Закрепление/удаление — `togglePinNote()`/
  `deleteNote()` (может любой, без проверки автора). Добавление Ctrl+Enter.
- Списки: покупки 🛒 и дела ✅, счётчики «в работе».
- Фото: загрузка до 10 файлов за раз, лайтбокс, закрепление ⭐, **лейблы 🏷**:
  у фото несколько лейблов, фильтр сверху («Все фото» или один лейбл), выбор
  нескольких фото (кнопка ○ на карточке) и добавление лейбла сразу всем,
  удаление лейблов без вреда фото.
- Песня: `<audio>` грузит `music/nasha-pesnya.mp3`; если нет — подсказка.

### Витрина «📅 События» и удаление фото
- `renderEventBar()` вызывается из `renderPhotos()` (перед сеткой). Витрина видна,
  только когда `currentLabel === EVENT_LABEL`.
- Кнопки-уровни: `#eventYears` (годы, по убыванию), `#eventMonths` (месяцы
  выбранного года), `#eventTitles` (события выбранного месяца). Каждый уровень
  `display:none`, пока у него нет кнопок или не выбран родитель. На кнопках —
  счётчик фото: `eventPhotosCount(year, month, title)` считает уникальные фото из
  галереи, чьи события (`eventsForPhoto(data)`) подходят под фильтр.
- Один делегат клика на `document` обслуживает все уровни; повторный клик по
  активной кнопке сбрасывает этот уровень (и более глубокие); «✕ Сбросить»
  (`data-ev-reset`) сбрасывает весь `eventFilter`.
- `deletePhoto(id)`: удаляет из `db.photos` + вычищает dataURL из `ev.photos`
  всех событий (пустые массивы убирает) + `renderCalendar()` — удалённое фото не
  остаётся «мёртвой» миниатюрой в панели дня.

### Комплимент дня и таймер
- `renderCompliment()` — карточка под hero. Фраза выбирается **на весь день** по хэшу
  `toDateString()` (массив `COMPLIMENTS`, 20 штук) — при перезагрузке не прыгает.
- `renderCountdown()` — таймер до **ближайшего будущего** события/свидания
  (`nextTarget()` перебирает события и свидания, учитывая время свиданий).
  Тикает каждую секунду (`tickCountdown()` → `#countdownTick`). Если событий нет — скрыт.

### Тёмная тема
- Кнопка `#themeToggle` в шапке и `#settingsThemeBtn` в настройках → `toggleTheme()`.
- Значение в `localStorage['universe_theme']`, атрибут `data-theme` на `<html>`.
- В `styles.css` у `:root` базовые CSS-переменные; `[data-theme="dark"]` переопределяет
  палитру + точечные корректировки тёмных карточек.

### Хотелки (wishlist)
- Вкладка «Хотелки»; модалка `#wishOverlay`: **текст обязателен**, фото и ссылка — нет.
  **Селекта «для кого» нет** — хотелка всегда в список вошедшего (`owner: getUser()`
  в `saveWishFromModal()`).
- Вишлист **разделён по людям**: две секции «👦 Хотелки Гоши» и «👧 Хотелки Даши»
  (фильтр по `owner`, сортировка: невыполненные сверху). Секции «хочу подарить» нет —
  подарок это сюрприз, его не показываем в списке.
- `wishCard()` — карточка: фото (клик → лайтбокс через `data-photo`), ссылка (target=_blank),
  отметка «исполнено» ✅ (`data-wish-done`) и удаление.

### Конфетти
- `celebrate()` — 36 частиц 💜💖✨🎉🌸💞 падают через весь экран (класс `.confetti`).
- Срабатывает: (1) когда оба ответили «да» на свидание; (2) на главной, если сегодня
  событие (`rem.some(days===0)`) — **один раз в день** (флаг в sessionStorage).

### Настройки: бэкап и место
- `renderSettings()` — сколько занято в браузере (JSON размер) и сколько времени прошло
  с последней копии (`db.backupDate`). Если копии не было **30+ дней** — жёлтое
  предупреждение. Экспорт обновляет `backupDate`.

### Летающие сердечки
- `spawnHeart()` (💜💖💕🌸✨) теперь: раз в **3.8с**, **только на главной** и
  **пауза, пока открыта модалка** (`.overlay:not([hidden])`). Живут 12с.

## 6. Тестирование

Смоук-тест **живёт в проекте**: `tests/uni-smoke.js` (203 проверки) +
`tests/repro-event-btn.js` (15 проверок, регрессия «＋ Добавить дату»).

Запуск (из папки проекта):
```powershell
node build.js              # app.js собирается из src/ (тест гоняет собранный файл)
node tests\uni-smoke.js app.js
```
или `npm test`, или двойным кликом по `run-tests.cmd` (сборка + тест).

- Тест ставит заглушки DOM/localStorage, оборачивает `app.js` в `new Function`
  и гоняет проверки: миграция старых данных (version=4), тема, комплимент, таймер,
  хотелки, лейблы/выбор фото, бэкап-напоминание, редактирование событий,
  ответы на свидания, слоты парящих фото, все вкладки, а также **сейф и вход**
  (создание, неверный/верный пароль, блокировка, пароль партнёра, смена пароля,
  экспорт/импорт сейфа и legacy-бэкапа) и **модалки свиданий/хотелок**
  (запись всегда от имени вошедшего, «Вместе» не создаётся).
- Счётчик «дней вместе» считается **динамически** от текущей даты — тест не устаревает.
- Синтаксис: `node --check app.js`.

**Важно:** тест запускается на реальном `app.js` (собранном из `src/`) в песочнице,
браузер не нужен, но DOM-события (`addEventListener`) в песочнице — заглушки,
поэтому проверяются рендеры и состояние, а не клики.

## 7. История изменений (последние)

-1. **Прошлые свидания в «Память» и «В этот день» (08.08.2026):**
   - 🔑 **Кнопка done в календаре:** панель дня → у свидания кнопка
     «✅ Свидание прошло» / «💗 Снять отметку» (`data-done-date`), общая
     `toggleDateDone(id)` с главной. Прошлое свидание теперь можно пометить
     выполненным прямо в календаре — и оно появляется в «Памяти»/«В этот день».
   - 📋 **Кастомные списки (схема v8):** `db.lists` вместо жёстких
     «Покупки/Дела»; миграция переносит старые данные, легаси-поля очищаются.
     Создание списка, подзадачи (добавить/выполнить/снять/удалить),
     «✔ Выполнить список» с подтверждением.
   - 🗓 **Свидания в «В этот день»:** даты свиданий прошлых лет — чипами
     в блоке «Наша история» рядом с событиями.
   - 🖼 **Сворачивание фото в «Памяти»:** в ряду 3 фото + «Показать ещё N»;
     скрытые фото остаются в DOM (`display:none`), лайтбокс работает по всем.
   - 🧪 Тесты: **339 (+28) в uni-smoke** + 18 dnd + 15 repro = **372**.

0. **Ревью и фиксы (07.08.2026):**
   - **P0 — защита данных:** все обращения к localStorage — через `store`
     (try-catch; переполнение → ненавязчивый тост `#appToast` вместо молчаливой
     потери), глобальные `window.onerror`/`unhandledrejection` → тост;
     `createVault` больше не оставляет «полусозданный» сейф при нехватке места.
   - **P0 — XSS:** `esc()` теперь и на `data`-URL фото во всех `src`/`data-photo`
     (защита от поддельного бэкапа); для wish-ссылок — `safeUrl()` (только
     http/https, `javascript:` → заглушка `#`).
   - **P1 — даты:** единый `parseLocalIso()` вместо `'T00:00:00'`-хаков —
     устойчиво к смене часового пояса.
   - **P1 — a11y:** `:focus-visible` для клавиатуры (мышь кольцо не получает),
     тост с `role=status`/`aria-live`.
   - **Модули:** монолит `app.js` разбит на 10 модулей `src/*.js` + `build.js`
     (побайтово эквивалентная сборка; `node --check` всех модулей чистый).
   - **Тесты:** +17 проверок в `uni-smoke.js` → 203 (deletePhoto чистит события,
     витрина «год→месяц→событие», смена пароля с неверным текущим, safeUrl).

## 7. История изменений (последние)

1. **Большой апдейт (по пунктам):**
   - 💌 Комплимент дня на главной (новый каждый день, стабилен для дня).
   - ⏳ Таймер до ближайшего события/свидания (тикает каждую секунду).
   - 🎁 Вишлист: описание обязательно, фото и ссылка — опционально;
     секции «хочу получить» / «хочу подарить», отметка «исполнено».
   - 🎉 Конфетти: оба «да» на свидание + в день события (раз в день).
   - 🌙 Тёмная тема (переключатель в шапке и настройках, CSS-переменные).
   - 💜 Сердечки: реже (3.8с), только на главной, пауза при открытых модалках.
   - 💾 Версия схемы данных (`DB_VERSION=3`) + `migrateDB()` для старых данных.
   - ⏰ Напоминание о резервной копии (30+ дней) + счётчик занятого места.
   - ✏️ Редактирование событий календаря (модалка-правка через `editingEventId`).
   - 📁 Фото: лейблы (несколько на фото), выбор нескольких фото для массового
     добавления лейбла, удаление лейблов без вреда фото, drag&drop порядка (поле `order`).
   - 📱 Мобильная лента фото под главной (показывается на узких экранах).
   - ✅ Смоук-тест перенесён в проект (`tests/uni-smoke.js`, 57 проверок,
     `run-tests.cmd` для запуска).
2. **Отзыв: таймер и вишлист.**
   - 🐛 Таймер показывал «…»: `nextTarget()` не передавал время в объект —
     теперь `countdownTarget` реально заполняется, тикер сразу считает. Тест
     ловит регресс («таймер тикает»).
   - 🎁 Вишлист переделан: секция «хочу подарить» **убрана** (подарок — сюрприз),
     теперь две секции по людям: «👦 Хотелки Гоши» и «👧 Хотелки Даши».
     В модалке вместо «Это…» — селект **«Для кого хотелка?»** (`#wishOwner`,
     по умолчанию текущий пользователь). Поле `type` в данных игнорируется.
   - 🌙 Тёмная тема: исправлено «сливание» текста — добавлены оверрайды для
     `.user-btn` (оверлей переключения пользователей), `.resp-btn`, `.cal-today`,
     `.day-event`, `.date-evt`, `.date-dot`, `.note.pinned`, `.dates-empty`,
     `.cal-nav:hover`.
   - 🧭 Активное меню стало заметнее (скейл + свечение + жирнее).
3. **Парящие фото**: привязаны к hero через `.hero-zone` (раньше могли наезжать
   на контент). 5 несимметричных слотов, заполняются 3 случайных, плавная смена,
   `pointer-events:none`, скрыты на мобильных (вместо них — лента фото).
3. **Свидания**: пригласивший видит «💌 позвал/позвала» без кнопок; отвечает
   только приглашённый (или оба при «вместе»). Оба «да» → конфетти.
4. **Smoke-тест** расширен до 57 проверок — все зелёные.
5. **Фото: лейблы вместо альбомов.**
   - 🏷 У фото теперь **несколько лейблов** (`labels` — массив), вместо одного альбома.
     Общий список лейблов живёт в `db.labels` (кнопки-фильтры сверху).
   - 🖼 Фильтр: «Все фото» или один лейбл (выбирается один; повторный клик не нужен).
   - ✂️ Переименование фото **убрано** (нигде не видно — бессмысленно).
   - ✅ Выбор нескольких фото кнопкой ○ на карточке: панель «Выбрано: N» → «🏷 Добавить
     лейбл» (новый или готовый из списка). Удаление лейбла ✕ — фото не трогает.
   - 🔄 Миграция `DB_VERSION=3`: старый `album` старых фото становится первым лейблом,
     `labels` собирается из всех фото.
   - 🌙 Тёмная тема: активное меню теперь красится градиентом — оверрайд
     `[data-theme="dark"] .nav-btn.active` (раньше специфичность `[data-theme="dark"] .nav-btn`
     перебивала градиент, оставалась только подсветка).
6. **Аудит 07.08.2026: цвета, анимации, логика (по пунктам):**
   - 🐛 **Календарь:** выбранный день больше не «залипает» при листании месяцев —
     `selectedDate` сбрасывается в `calPrev`/`calNext`; `calY/calM` инициализируются
     при объявлении (нет «undefined undefined»).
   - 🐛 **Импорт бэкапа** теперь прогоняет `migrateDB()` — старая схема получает
     новые поля (`version`, `labels`, `wishlist`, `backupDate`).
   - ⏳ **Таймер** после наступления цели сразу пересчитывает следующую
     (`tickCountdown()` → `renderCountdown()`), а не зависает на «Уже наступило!».
   - 🌈 **Контраст WCAG AA:** `--muted` темнее; мелкий розовый текст
     (`.resp-me`, `.day-sub`, `.compliment-text`, `.both-yes`) — на `--pink-ink`
     (#be185d на светлом, #f9a8d4 на тёмном); фиолетовый — `--violet-ink`;
     градиенты (`--grad`, `.btn-danger`, `.btn-date`, `.resp-btn.no.on`,
     `.tag-today`) затемнены, белый текст читается.
   - 📅 **29 февраля** в невисокосные годы корректно «съезжает» на 28 февраля
     (`nextOcc()`).
   - 🛡 **Экранирование:** эмодзи из событий/свиданий/хотелок обёрнуты `esc()`.
   - ⌨️ **Клавиатура и a11y:** Esc закрывает модалки, Enter/пробел выбирают день
     календаря (ячейки `tabindex="0"`), у кнопки темы `aria-pressed`.
   - 🌙 **Системная тема** при первом запуске (`getTheme()` → `matchMedia`),
     дальше — как раньше, вручную.
   - 🖼 **Мобильная лента** фото сортируется как основная сетка (`photoSort`).
   - 🎵 **Песня:** подсказка меняется на «▶ Нажми play», когда файл готов.
   - 🕐 **`input[type=time]`** получил общий стиль полей.
   - ⏸ **Энергия:** таймеры и смена парящих фото паузаятся в скрытой вкладке
     (`isHidden()`), сердечки не спамят в фоне.
   - 🧪 Смоук-тест расширен до **60 проверок** (регрессии: заголовок месяца без
     «undefined», клавиатурная навигация, авто-пересчёт таймера).
7. **Зашифрованный сейф + вход (пароли):**
   - 🔐 Все данные шифруются AES-GCM и хранятся в `universe_vault`
     (`{ ver:1, db, keys:[{who, k, s, iter, iv, a}] }`). Первый запуск мигрирует
     открытый `universe` внутрь сейфа и удаляет его.
   - 🧂 **Починен вход после перезапуска:** PBKDF2-salt теперь **сохраняется в wrap**
     (`keys[].s`) в `createVault`, `savePassFor`, `changePass`. Раньше salt не хранился —
     ключ после перезапуска восстановить было невозможно.
   - 🔑 При разблокировке AES-key импортируется как **extractable** (`true`) — без этого
     не работал экспорт ключа (wrap) для пароля партнёра.
   - 👥 Пароль партнёра (`savePassFor`), смена своего пароля, замок/разблокировка,
     экспорт/импорт сейфа, импорт **старых открытых бэкапов** (сразу шифруются).
8. **Модалки свиданий/хотелок: «кто» убран полностью.**
   - ✂️ Селекты «Кто приглашает?» (`dtFrom`, включая «💜 Вместе») и «Для кого хотелка?»
     (`wishOwner`) удалены из HTML и JS. Свидание: `from: getUser()`; хотелка:
     `owner: getUser()` (функции `saveDateFromModal()` / `saveWishFromModal()`).
     Под Гошей нельзя назначить свидание от имени Даши или «Вместе», и наоборот.
   - ♻️ Старые свидания `from:'both'` по-прежнему рендерятся (легаси-совместимость).
   - 🧪 Смоук-тест обновлён: проверяет **факт записи** от имени вошедшего и что
     «Вместе» больше не создаётся. Итог — **102 проверки**, все зелёные.
9. **Главная: блок «Скоро важное событие» удалён.**
   - ✂️ На главной было два «таймера» до ближайшего события: живой отсчёт `#countdown`
     (до единственной следующей цели) и список напоминаний `#reminders` («сегодня! /
     завтра! / через N дн.», события в пределах 14 дней). Напоминания дублировали
     таймер — блок убран из `index.html`, `renderHome()` и стилей (`.rem`,
     `.rem-today`, `.rem-empty` + их dark-варианты).
   - 💌 Конфетти в важный день (`maybeCelebrateAnniversary`) работает как раньше:
     список ближайших по-прежнему считается в `renderHome()` для проверки «сегодня!».
   - 🧹 Пустое состояние списка свиданий переименовано из `rem-empty` в `dates-empty`
     (стили сохранены, включая тёмную тему).
   - 🧪 Тесты зелёные: **102/102**.
10. **Календарь: быстрый выбор, длительные события, фото в событиях, ховер.**
   - ⚡ **Быстрый выбор месяца/года:** селекты `#calMonthSelect`/`#calYearSelect`
     над календарём (`fillCalJump()` заполняет опции один раз, `jumpCalendar(m, y)`
     переключает и рендерит). Листать по одному месяцу до нужного года больше
     не нужно — стрелки `‹ ›` остались для соседних месяцев.
   - 📅 **Длительные события:** в модалке события появилось поле «До» (`#evEnd`).
     `eventsOn()` для не-повторяющихся событий матчит каждый день диапазона
     `date..endDate`; ячейки промежутка получают класс `in-span` (подсветка,
     светлая + тёмная тема); в панели дня у названия подпись «до N мес.»;
     `repeat` для долгих событий принудительно `false` (и галочка снимается
     автоматически при вводе даты окончания). Таймер/главная работают по дате
     начала (`nextOcc`), ничего не сломано.
   - 📷 **Фото в событиях:** кнопка «📷 Добавить фото» (`#evPhoto`, до 5 файлов).
     При сохранении (`saveEventFromModal()`) фото кладутся в общую галерею
     `db.photos` с **лейблом = названию события** (создаётся в `db.labels`, если
     его не было) и сохраняются в `ev.photos`. В панели дня — миниатюры
     (`.ev-thumb`), клик открывает лайтбокс. Повторное сохранение не дублирует
     фото (дедуп по data-URL, лейбл лишь добавляется).
   - 🖱 **Ховер не перебивает выбранный день:** `.cal-cell:not(.selected):hover`
     (было `.cal-cell:hover`) — подсветка при наведении только у невыбранных дней,
     выбранный сохраняет градиент. Аналогично в тёмной теме.
   - 🧪 Смоук-тест расширен до **118 проверок** (jumpCalendar, диапазон,
     фото/лейбл события, панель дня).
11. **Календарь: баг «длительное событие не появляется в календаре» — исправлен.**
- [x] Событие «не появлялось» после сохранения через кнопку «＋ Добавить дату» — клик передавал объект MouseEvent
  как id в `openEventModal()`, поэтому модалка открывалась в режиме «✏️ Изменить дату» с несуществующим id,
  а `saveEventFromModal` молча терял событие. Фикс: обработчик обёрнут в `() => openEventModal()`,
  принимается только строковый id (`typeof id === 'string'`), при ненайденном редактируемом событии создаётся новое,
  клик по фону оверлея тоже сбрасывает `editingEventId`. Покрыто тестами `tests/repro-event-btn.js` и регрессией в `uni-smoke.js`.

   - 🐛 **Причина:** после сохранения события календарь оставался на том месяце,
     который был открыт. Если событие добавляли на другой месяц (отпуск в
     сентябре, пока на экране август) — оно уезжало «за кадр»: в БД есть, а на
     сетке не видно. Проверено воспроизведением в headless-Chrome.
   - ✅ **Фикс:** `saveEventFromModal()` после сохранения ставит `calM/calY` на
     месяц даты события и выделяет день начала (`selectedDate`). Событие сразу
     видно и на сетке, и в панели дня — даже если его добавили не в открытом месяце.
   - 🛡 **Защита от перепутанных дат:** если «До» раньше начала — событие не
     сохраняется, алерт «Конец события не может быть раньше начала 💜»
     (раньше такое событие молча становилось однодневным).
   - 🧪 Смоук-тест расширен до **122 проверок**: перескок на месяц события +
     отклонение перепутанных дат. Проверено в реальном браузере (headless-Chrome).
12. **Календарь: UI — селекты вместо дубля месяца/года, названия в ячейках, 📷 в панели дня.**
   - 🔤 **Убран текст-заголовок** `#calTitle` («Август 2026») — месяц/год показывают
     только селекты, перенесённые в шапку `.cal-head` между стрелками `‹ ›`
     (`index.html`, `.cal-jump-row{flex:1;justify-content:center}`). В `renderCalendar()`
     больше нет `$('#calTitle').textContent = …`.
   - 🏷 **В ячейках календаря видно название события** рядом с эмодзи: `💜 Название`.
     До 2 событий на день + «+N» для остальных; длинные названия обрезаются
     многоточием (`cal-dot` — `white-space:nowrap` + `text-overflow:ellipsis`,
     фон-плашка; тёмная тема учтена). Свидания по-прежнему 💘 (`date-dot`).
   - 📷 **Быстрое добавление фото** из панели дня: у каждого события кнопка
     `data-photo-event` → `addEventPhotoQuick()` (временный `<input type="file">`,
     до 5 файлов). Логика «фото в галерею + лейбл = названию» вынесена из
     `saveEventFromModal()` в общую `addEventPhotosToGallery()` — модалка и быстрая
     кнопка работают одинаково, дублирования нет.
   - 🧪 Смоук-тест: проверки на `#calTitle` заменены на селекты, добавлены проверки
     «название события в ячейке» и «кнопка 📷 в панели дня». Итог — **129 проверок**,
     все зелёные.
13. **Модалка «Добавить дату» приведена к стилю сайта.**
   - 🎨 Модалка события (`#eventOverlay`, она же «Изменить дату») больше не выглядит
     «голой простынёй»: сверху градиентная шапка `.ev-head` в фирменном градиенте
     с белым заголовком и подзаголовком `#evHeadSub`, который меняется в зависимости
     от режима («Сохрани важный день для вас двоих 💞» / «Поправь детали — всё
     сохранится ✨»). Крестик — белая плашка поверх шапки.
   - 📅 «Дата» и «До» — в две колонки `.ev-grid` (как «Дата/Во сколько» у свидания),
     на узких экранах (≤420px) — одна колонка. Подсказка про диапазон — мягкая
     плашка `.ev-note` с пунктирной рамкой.
   - ✅ Розовый акцент чекбокса (`accent-color:#ec4899`), `color-scheme` для
     date-полей (в тёмной теме системная иконка календаря не «слепнет» на белом),
     тёмные варианты шапки и плашки. Кнопка «Сохранить 💜» растянута на всю ширину.
   - 🧪 Смоук-тест: +2 проверки (подзаголовок при создании и при правке).
     Итог — **131 проверка**, все зелёные.
14. **Кастомный date-picker — системный календарь браузера больше не показывается.**
   - 📅 `input[type=date]` открывал «голый» системный календарь (Windows/браузер),
     который не стилизуется. Вместо него — собственный попап `#datePop` в стилистике
     большого календаря: стрелки ‹ ›, селекты месяца/года (те же `MONTHS`), сетка
     Пн–Вс, подсветка «сегодня» и выбранной даты (фирменный градиент), кнопки
     «Сегодня»/«Очистить». Закрытие — клик мимо или Esc; попап сам не вылезает за
     край экрана.
   - ⚙️ Выбранная дата пишется в поле как ISO `YYYY-MM-DD` (+ события `input`/`change`),
     поэтому `saveEventFromModal`, `openEventModal` и модалка свидания работают без
     изменений. Нативный индикатор скрыт через `::-webkit-calendar-picker-indicator
     {display:none}`; на `focus` поля (`#evDate`, `#evEnd`, `#dtDate`) открывается
     свой календарь. Тёмная тема учтена.
   - 🧪 Смоук-тест: +7 проверок (привязка focus к полям, ISO-сборка, рендер сетки,
     выбор даты, закрытие). Итог — **138 проверок**, все зелёные.
15. **Фото событий больше не плодят лейблы: витрина «📅 События» в галерее.**
   - 🏷 Раньше каждое фото, прикреплённое к событию, получало лейбл = названию
     события — после 30+ событий фильтр лейблов засорялся. Теперь у всех фото
     событий один общий лейбл `EVENT_LABEL = '📅 События'`; название события
     остаётся **подписью** фото (`title`) и показывается в витрине.
   - 📅 В галерее появился чип «📅 События (N)» — отдельная витрина с селектом
     конкретного события и подписью названия под каждым фото (`.photo-caption`).
     Перетаскивание порядка работает и внутри витрины (`filteredPhotos()`).
   - 🛡 Служебный лейбл «События» нельзя удалить через ✕; из модалки «добавить
     лейбл» он скрыт. Старые данные мигрируют: `relabelEventPhotos()` (v4) ставит
     общий лейбл фото из `ev.photos` и убирает их лейблы-названия.
   - 🧪 Смоук-тест: +11 проверок (общий лейбл, чип, витрина, селект события,
     защита лейбла, v4-миграция). Итог — **149 проверок**, все зелёные.
16. **Витрина «📅 События»: фильтр кнопками «год → месяц → событие» + баг с удалением фото.**
    - 🎛 Вместо селекта `#eventFilterSel` (список всех событий разом) — **кнопки-уровни**:
      сперва годы, после клика по году появляются месяцы, после клика по месяцу —
      события. На кнопках счётчик фото (`eventPhotosCount()`), повторный клик по
      активной кнопке сбрасывает уровень, «✕ Сбросить» убирает всё. `eventFilter`
      стал объектом `{year, month, title}`; клики — один делегат на `document`
      (раньше — `change`-слушатель селекта). `renderEventBar()` показывает только
      события, у которых есть фото в галерее (`ev.photos` пересекается с `db.photos`).
    - 🐛 **Удалённое из галереи фото оставалось в событии**: удаление вынесено в
      `deletePhoto(id)` — чистит `ev.photos` во всех событиях (пустые массивы
      удаляет) и перерисовывает календарь; «мёртвых» миниатюр в панели дня нет.
    - 🧪 Смоук-тест: проверки селекта заменены на кнопки (появление месяцев/событий,
      подсветка выбранного, кнопка сброса) + тест удаления фото из события.
      Итог — **156 проверок**, все зелёные (плюс `repro-event-btn.js`: 15).
17. **Вишлист «исполнено другим» + drag&drop фото на лейблы.**
    - 🎁 Хотелку может исполнить **только партнёр** (свою нельзя — на карточке
      подсказка «Только … исполнит 💜»); на исполненной карточке бейдж
      «💜 Исполнено Гошей/Дашей · дата» (новые поля `doneBy`, `doneAt`, старые
      записи мигрируют автоматически — поля опциональны), снять отметку может
      только исполнивший, исполненные хотелки уходят вниз списка.
    - 🏷 Фото: кнопки лейблов в `#labelBar` стали целью drag&drop — при
      перетаскивании фото на чип лейбл получает перетаскиваемое фото **и все
      отмеченные** (если есть), чип подсвечивается классом `.drag-over`;
      служебный лейбл «📅 События» — не цель. Крестик ✕ на бейдже лейбла у фото
      убирает лейбл с конкретного фото (`removeLabelFromPhoto`). После применения
      лейбла (и других действий) выделение снимается — `applyLabelToSelected`
      чистит `selectedPhotos`. На телефонах остаётся прежний путь
      (выбор + модалка).
    - 🧪 Смоук-тест: +15 проверок (подсказка/кнопка/бейдж/правила снятия,
      исполненные внизу, `applyLabelToPhotos`, снятие выделения, ✕ с фото).
      Итог — **171 проверка**, все зелёные (плюс `repro-event-btn.js`: 15).
18. **Фикс drag&drop и автопоказ ближайшего события (07.08.2026).**
    - 🐛 **Заметки не менялись местами при drag&drop.** Причин было две: (1) в
      `dragover` не задавался `dropEffect='move'` — Chrome может отменить drop,
      если dropEffect не согласован с `effectAllowed`; (2) бросок на **фон списка**
      (ниже последней карточки / между карточками) игнорировался — `closest('.note')`
      возвращал `null` и перестановки не было. Теперь: `dropEffect='move'` явно,
      `noteDropPosition()` определяет место по краю (выше первой / ниже последней,
      `reorderNoteIds()` — чистая функция), бросок на саму заметку ничего не ломает.
    - 🐛 **Drag фото на лейбл: чип подсвечивался, но лейбл не навешивался.**
      У фото в `dragstart` было `effectAllowed='move'`, а у чипа в `dragover` —
      `dropEffect='copy'`; 'copy' не входит в 'move', поэтому Chrome отменял drop
      (dragenter/подсветка работали). У фото теперь `effectAllowed='copyMove'`,
      у сетки фото `dropEffect='move'`, у лейбла `dropEffect='copy'`.
    - ✨ **Обратное направление:** чип лейбла стал `draggable` — его можно
      перетащить прямо на фото (навешивается на фото и все отмеченные).
    - 📅 **Ближайшее событие показывается автоматически:** открытие вкладки
      календаря само вызывает `showNearestEvent()` — плашка `#jumpInfo` видна
      всегда (в т.ч. когда событие в текущем месяце), прыжок — если событие вне
      текущего месяца; кнопка «⏭» сохранена. *(Поведение переработано в п.23:
      вкладка открывается на текущем месяце, кнопка/плашка только вне месяца
      ближайшего события.)*
    - 🧪 Новый тест `tests/uni-dnd.js` — мини-DOM с событиями (`dragstart`/
      `dragover`/`drop`/`dragenter`): 18 проверок (заметки в т.ч. на фон, фото→лейбл,
      чип→фото, фото→фото). Смоук: +6 (плашка в текущем месяце, автопрыжок при
      открытии, `reorderNoteIds`). Итог — **193 + 18 + 15 проверок**, все зелёные.
19. **Фикс битых фото на главной и после удаления фото (07.08.2026).**
    - 🐛 **Сразу после входа на главной — битые иконки.** `warmThumbCache()`
      прогревает кэш миниатюр асинхронно, а первый рендер уже выводил картинку с
      `src=""`, потому что `photoSrc(p)` ещё возвращал пустую строку (кэш холодный).
    - ✅ `warmThumbCache()` теперь по завершении прогрева сам перерисовывает
      `renderHome()`, `renderPhotos()` и `renderCalendar()` — пустые `src`
      заполняются URL из прогретого кэша.
    - ✅ `renderFloatingPhotos()` при пустом `photoSrc(p)` не ставит `src=""`,
      а прячет картинку (`visibility: hidden`) — битой иконки не видно вовсе.
    - ✅ `renderMobilePhotos()` при холодном кэше рендерит заглушки
      `<img data-photo-src>`; `renderHome()` зовёт `hydratePhotoImgs(...)`,
      который асинхронно заполняет src.
    - 🐛 **После удаления фото ломались ВСЕ остальные.** `deletePhoto()` вызывал
      `clearThumbCache()` — сбрасывался весь кэш миниатюр, и все фото на главной и
      в галерее превращались в битые. Теперь удаляется только запись конкретного
      фото (`thumbCache.delete(id)`) и перерисовывается `renderHome()`.
    - 🧪 Итог — **259 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 292
      проверки**, все зелёные.
20. **Фаза B (душевные фичи) + hash-роутинг + EXIF-дата фото (07.08.2026).**
    - ✅ **EXIF-дата при загрузке фото:** в `70-photos.js` при добавлении в галерею
      теперь вызывается `extractExifDate(f)`; результат (`takenAt`) попадает в запись
      фото и в мету `photoStore`. Это закрывает хвост Фазы A и даёт «В этот день»
      настоящую дату снимка.
    - ✅ **Hash-роутинг (#/view)**: `go(view)` пишет `location.hash`, работает кнопка
      «назад» в браузере и прямые ссылки вида `index.html#/notes`; навигация вынесена
      в `showView(view)` (неизвестная вкладка игнорируется). В песочнице тестов
      `location` нет — показ остаётся синхронным.
    - ✅ **«В этот день»** (виджет на главной): события, заметки и фото, у которых
      день+месяц совпадают с сегодняшними из прошлых лет. Фото — **только по EXIF
      или дате события** (решение пользователя: дата загрузки не считается).
    - ✅ **Кольцо прогресса отношений** (`renderProgressRing`): SVG-кольцо на главной,
      % до ближайшей годовщины + дни вместе.
    - ✅ **Трекер настроения:** `db.moods` (одна отметка на день, 1–5), виджет «Как
      прошёл день?» на главной + годовой хитмап по неделям (`renderMoodHeatmap`).
    - ✅ **Лента «Память»**: новая вкладка и новый модуль `src/35-memory.js` —
      хронологическая лента из событий, свиданий, заметок, фото и настроений
      (`memoryItems`/`renderMemory`).
    - 🧪 Итог — **279 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 312
      проверки**, все зелёные.
21. **Фаза B: редизайн «Память» + удаление настроений + настройки (08.08.2026).**
    - ❌ **Удалён трекер настроения** полностью: «Как прошёл день?» (виджет на
      главной), годовой хитмап, `db.moods`, `setMood`/`moodFor`/`renderMoodBox`/
      `renderMoodHeatmap`, все mood-стили и тесты. В `index.html` удалены контейнеры
      `#moodBox` и `#moodHeatmap`.
    - ✅ **«Память» → таймлайн-дерево:** `memoryItems` заменён на `memoryByDay()`,
      который группирует артефакты по дням. Карточки чередуются слева/справа от
      центрального ствола (CSS `.tl-stem`). Фото — горизонтальная прокрутка
      (1 блок на день), события — блоки с привязанными фото, свидания — только
      `done === true` (отменённые не попадают). Заметки и фото без EXIF/события
      в «Память» НЕ попадают. Адаптив: на ≤640px все карточки справа от ствола.
    - ✅ **Кольцо прогресса:** `max-width` увеличен с 640px до 680px (теперь
      совпадает с другими блоками на главной).
    - ✅ **Настройки: пароль партнёра** — кнопка «🔑 Добавить пароль для партнёра»
      теперь **всегда видна** (даже когда оба пароля заданы), метка меняется на
      «🔑 Сменить пароль партнёра».
    - 🧪 Итог — **278 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 311
      проверки**, все зелёные.

22. **Фаза C: память, календарь, свидания (08.08.2026):**
    - 🐛 **Наша история** — убран `max-width:640px` у `.progress-ring` (блок был
      уже остальных карточек на главной).
    - 🐛 **Память: будущие события/свидания** — `memoryByDay()` теперь фильтрует
      по дате: события/свидания попадают в таймлайн только если их дата ≤ сегодня.
      «Грузия» (октябрь) больше не отображается заранее.
    - 🐛 **Память: удалённые свидания** — `datesOn()` теперь возвращает ВСЕ
      свидания (включая `done:true`), чтобы календарь показывал завершённые.
      Домашний блок «Наши свидания» фильтрует `!done` — на главной только
      предстоящие. Удалённые (крестик ✕) — удаляются из `db.dates` и не
      попадают никуда.
    - 💞 **Свидания: фото** — к свиданиям можно прикрепить фото (кнопка 📷
      в панели дня календаря). Фото свиданий попадают в общую галерею
      под системным лейблом `DATE_LABEL = '💞 Свидания'` (видна в витрине
      фото, защищена от удаления/drag-drop, как `📅 События`).
    - 💞 **Свидания: логика «прошло»** — кнопка 💗 теперь помечает
      `done:true` (свидание убирается с главной, но остаётся в календаре
      с визуальным индикатором ✅ и зачёркнутым текстом).
    - 💞 **Память: фото свиданий** — завершённые свидания с фото отображают
      миниатюры в таймлайне памяти.
    - 🏷 **DATE_LABEL** (`'💞 Свидания'`): добавлен в `00-core.js`, защищён
      от удаления и перетаскивания в лейблах (как `📅 События`).
    - 📦 **DB_VERSION → 7** (фото свиданий — поле `photos` в `dt`).
    - 🧪 Новые тесты: будущее событие не попадает в память, будущее свидание
      (done:true) не попадает, удалённое свидание не попадает.
    - 🧪 Итог — **281 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 314
      проверки**, все зелёные.

23. **«Наша история»: коллаж фото + статистика; календарь без автопрыжка (08.08.2026).**
    - 🖼 **Парящие фото вокруг hero убраны** (`#floatPhotos`, `renderFloatingPhotos`,
      `pickFloating`, CSS `.float-photo`/`floaty`, интервал 7с). Герой чистый.
    - 🖼 **Коллаж переехал в блок «Наша история»** (`#progressRing`): до 3 случайных
      фото с асимметрией — разный размер (72–100px), поворот (`rotate:`), верхний
      сдвиг и лёгкое парение (`histFloat`). `HISTORY_PHOTO_SLOTS` +
      `pickHistoryPhotos()` (ГПСЧ `mulberry32`, seed по дате → коллаж стабилен
      весь день) + `historyPhotosHtml()`. Фото кликабельны (lightbox, `data-photo`),
      при холодном кэше — `data-photo-src` + `hydratePhotoImgs($('#progressRing'))`.
    - 📊 **Статистика под кольцом:** чипы `.hs-chip` — 📸 фото / 📅 событий /
      💘 свиданий / 📝 заметок / 🎁 хотелок (новый хелпер `plural()`).
    - 📅 **Календарь: открытие вкладки → текущий месяц**, без прыжка и без
      автовыбора дня (раньше прыгал на месяц ближайшего события).
    - 📅 **Кнопка «⏭ К ближайшему событию»:** `updateNearestJump()` (зовётся из
      `renderCalendar`) показывает кнопку + плашку `#jumpInfo`, только когда
      ближайшее событие/свидание **НЕ в показываемом месяце**; в месяце самого
      ближайшего события и кнопка, и плашка скрыты. Даже в месяце с другими
      событиями, но не с ближайшим — кнопка есть. `showNearestEvent`/`hideJumpInfo`
      удалены; `jumpToNearestEvent()` прыгает, выделяет день, и плашка скрывается.
    - 🧪 Смоук-тест: коллаж (2 из 3 слотов, асимметрия, fallback, статистика,
      стабильность за день), календарь (+7: кнопки нет/есть в нужных месяцах,
      плашка рассказывает о ближайшем, открытие вкладки без прыжка).
      Итог — **288 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 321
      проверки**, все зелёные.

24. **Блок «Наша история»: фото «в этот день», прогресс хотелок, кнопка 🎲 (08.08.2026).**
    - 📅 **Фото «в этот день» в коллаже:** `onThisDayPhotos()` (через `onThisDayItems()`,
      EXIF/дата события/свидания) встают первыми в слоты коллажа, остальные — случайные.
      Когда такие фото есть — на коллаже бейдж `✨ В этот день` (`.hp-badge`).
    - 🎲 **Кнопка «Перемешать»:** `shuffleHistoryPhotos()` заново тасует случайную часть
      (фото «в этот день» не трогает), результат фиксируется до конца дня
      (`historyCollage = {day, sig, ids}`). Кэш коллажа инвалидируется сигнатурой
      состава галереи `sig` — при добавлении/удалении фото коллаж пересобирается.
      Кнопка подключена делегированием на `#progressRing` (click + Enter/Space).
    - 🎁 **Прогресс хотелок:** вместо чипа-счётчика — `.hs-wish` с `🎁 3/7` и
      мини-полоской `.hs-bar` (ширина = % исполненных из `db.wishlist`).
    - 🧪 Смоук-тест (+13): приоритет «в этот день», бейдж, стабильность, перемес
      (3 фото, phOtd на месте, день зафиксирован, обработчик подключён),
      прогресс 2/3 → полоска 67%, обновление при изменении списка (2/4),
      пустой вишлист → «🎁 пока пусто».
      Итог — **301 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 334
      проверки**, все зелёные.

25. **Дебак: без дублей и битых фото (08.08.2026).**
    - 🧹 **Отдельный виджет «В этот день» удалён** — он дублировал коллаж «Наша
      история». Теперь события прошлых лет показываются чипами `.history-otd`
      прямо в блоке «Наша история» (только когда «в этот день» что-то есть);
      фото — как раньше, в коллаже с бейджем `✨ В этот день`.
    - 🐛 **Память: фото больше не дублируются** — `memoryByDay()`: фото,
      привязанные к событию/свиданию, показываются только в их карточках,
      а в сетку дня попадают только свободные фото с EXIF (`p.takenAt`).
    - 🐛 **Память: фото больше не скрыты** — ленты `.tl-photos`/`.tl-item-photos`
      переведены с горизонтального скролла на перенос строк (`flex-wrap`):
      все фото видны на ПК, «скрытой» части нет.
    - 🐛 **Удаление фото чистит и свидания** — `deletePhoto()` теперь убирает id
      и из `dt.photos` (раньше в панели дня календаря оставалась рамка с битым
      файлом). Дополнительно `evThumbs`/`dtThumbs` пропускают «мёртвые» id
      (легаси data-URL показываются напрямую).
    - 🧪 Смоук-тест (+10): события «в этот день» в блоке истории, виджет
      `#onThisDay` больше не рендерится, фото события/EXIF не дублируются
      в сетке дня, `deletePhoto` чистит `dt.photos`, мёртвые id не рисуют
      миниатюру, легаси data-URL показывается.
      Итог — **311 (uni-smoke) + 18 (uni-dnd) + 15 (repro-event-btn) = 344
      проверки**, все зелёные.

## 8. Известные ограничения / TODO

- [ ] Данные живут только в одном браузере на одном компьютере (нет облака).
- [x] Сердечки слишком частые / летают при модалках — сделаны реже + пауза.
- [x] События нельзя редактировать — добавлено ✏️.
- [x] Календарь: событие «не появлялось», если добавляешь его на другой месяц — календарь сам переходит на месяц события после сохранения.

- [x] Нет тёмной темы — добавлена 🌙.
- [x] Нет версии схемы данных — добавлены `DB_VERSION` + `migrateDB()`.
- [x] Парящие фото вокруг hero заменены на коллаж внутри блока «Наша история» (до 3 фото, асимметрия, стабилен в течение дня) + статистика; мобильная лента осталась.
- [x] Блок «Наша история»: фото «в этот день» (EXIF/дата события) в коллаже с бейджем ✨,
  кнопка «🎲 Перемешать» (фиксирует выбор на день), прогресс хотелок `🎁 3/7` с полоской.
- [x] Отдельный виджет «В этот день» удалён (дублировал коллаж) — события прошлых лет
  теперь чипами в блоке «Наша история»; фото в «Памяти» не дублируются и не скрываются;
  удаление фото чистит и свидания (нет битой рамки в панели дня календаря).
- [ ] Облачная синхронизация (телефон ↔ компьютер) — самый большой будущий шаг.
- [x] Календарь: выбранный день «залипал» при листании месяцев — сбрасывается.
- [x] Календарь: быстрый выбор месяца/года (без 12 кликов).
- [x] Календарь: длительные события (путешествия на несколько дней) — поле «До» + подсветка диапазона.
- [x] Календарь: фото в события (лейбл = названию, фото дублируется в галерею).
- [x] Календарь: ховер не перебивает выбранный день (`:not(.selected):hover`).
- [x] Импорт бэкапа не прогонял миграции — теперь `migrateDB()`.
- [x] Контраст мелкого текста/кнопок — подтянут к WCAG AA.
- [x] Календарь: месяц/год дублировались (текст + селекты) — оставлены только селекты в шапке.
- [x] Календарь: в ячейках видны названия событий (не только эмодзи).
- [x] Календарь: кнопка 📷 быстрого добавления фото в панели дня (без модалки).
- [x] Модалка «Добавить дату» выбивалась из стилистики — градиентная шапка, даты в 2 колонки, плашки-подсказки.
- [x] Системный календарь у полей дат — заменён на кастомный date-picker в стиле сайта.
- [x] Таймер застревал на «Уже наступило!» — сам переходит к следующей цели.
- [x] Esc/Enter, `aria-pressed`, пауза таймеров в скрытой вкладке, системная тема
  при первом запуске.
- [x] Вишлист: показывать «исполнено» другому, совместный режим (обе секции общие).
- [x] Таймер: возможность закрепить конкретное событие для отсчёта — решено НЕ
  делать (07.08.2026): ближайшего события достаточно, закрепление не нужно.
- [x] Тёмная тема: учитывать системную тему при первом запуске.
- [x] Фото: альбомы — заменены на лейблы (несколько на фото, массовое добавление).
- [x] Фото: перетаскивание фото прямо на кнопку лейбла (добавить лейбл drag&drop).
- [x] Вход в сейф после перезапуска не работал — salt теперь хранится в wrap (`keys[].s`).
- [ ] ⚠️ AES-ключ живёт в памяти как extractable (нужно для wrap пароля партнёра) —
  компромисс безопасности, пересмотреть при добавлении облака.
- [x] Фото: при удалении из галереи фото убирается и из события — в календаре
  не остаётся «мёртвых» миниатюр (`deletePhoto(id)` чистит `ev.photos`).
- [x] Фото: витрина «📅 События» — вместо селекта на все события фильтр кнопками
  «год → месяц → событие»: кнопки появляются по мере выбора, на кнопках счётчик
  фото, повторный клик сбрасывает уровень, есть «✕ Сбросить».
- [x] Фото: после действий с выбранными фото (навешивание лейбла и т.п.) выделение
  снимается; крестик ✕ на бейдже фото убирает лейбл с конкретного фото.
- [x] Вишлист: исполненные хотелки уходят вниз списка; свою хотелку исполнить нельзя —
  только партнёр, снять отметку может только исполнивший (бейдж «💜 Исполнено …»).
- [x] Заметки: drag&drop не менял местами (dropEffect + бросок на фон списка) —
  исправлено, бросок на пустое место ставит заметку в начало/конец.
- [x] Фото: drag на лейбл подсвечивал, но лейбл не навешивался (effectAllowed/
  dropEffect) — исправлено; чип лейбла теперь можно тащить прямо на фото.
- [x] Календарь: ближайшее событие показывалось только по кнопке — теперь кнопка «⏭»
  и плашка появляются автоматически в любом месяце БЕЗ ближайшего события, а в месяце
  самого ближайшего события скрыты; вкладка открывается на текущем месяце.
- [x] Дата-пикер: aria-паттерн «dialog + grid» — `role=dialog`, `aria-label` дней
  («9 августа 2026 года»), `aria-current` у сегодня, roving tabindex, клавиатура
  (стрелки, Home/End, PageUp/PageDown, Shift+PageUp/PageDown, Enter, Esc),
  озвучка даты для скринридера через `#dpLive`.
- [x] Настройки: переключатель «Отключить анимации» — `data-motion` на `<html>`
  (`reduced`/`full` перекрывают систему, без выбора — уважаем
  `prefers-reduced-motion`); сердечки/конфетти не создаются при отключённых.
- [x] Фото: дебаунс ре-рендеров галереи — несколько `renderPhotos()` в одном
  кадре схлопываются в один (`requestAnimationFrame`).
